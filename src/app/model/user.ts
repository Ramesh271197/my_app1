// this is a user model class which is used to define the structure of user object has properties like id, name, dob, email, phone, password, etc.
export interface User {
    id: number;
    name: string;
    dob: string;
    email: string;
    phone: string;
    password: string;
}
